object Pessoa{
    name: str,
    age: int,
}

component Sla {}

func main(): Component {
    let mut pessoa = Pessoa(
        name:"123", age: 123
    );
    pessoa.name = "Jorge";
    pessoa.age = 255;
    pessoa = Pessoa(name: "jorge", age:pessoa.age + 3);
    Sla{}
}
